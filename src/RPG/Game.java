package RPG;

public class Game {

    public static void start() {

        // Player playerOne = PlayerFactory.createCharacter();
        //Player playerTwo = PlayerFactory.createCharacter();

        TestingLayoutManagers myFrame = new TestingLayoutManagers();
       // BattleScreen battleScreen = new BattleScreen(playerOne.getName(), playerOne.getHealthPoints(), playerTwo.getName(), playerTwo.getHealthPoints());

    }

}